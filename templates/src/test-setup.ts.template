// Jest test setup file
import { logger } from './utils/logger.js';

// Set log level to ERROR to reduce noise during tests
// Use string-based log level since LogLevel enum may not be available as a value
try {
  (logger as any).setLevel?.('error');
} catch {
  // Fallback: logger may not support setLevel method
  console.warn('Logger setLevel method not available');
}

// Global test configuration
beforeEach(() => {
  // Reset any global state before each test
});

afterEach(() => {
  // Cleanup after each test
});